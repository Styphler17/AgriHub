Synchronize Names and Improve Account Creation

- Refactored Auth.tsx to collect First and Last names during signup, automatically creating a user profile on successful login.
- Modified handleUpdateUser in App.tsx to propagate name and profile image changes to all of a user's marketplace listings.
- Improved data consistency and personalization across the app.
- Added automatic cleanup of hardcoded mock listings on startup.
