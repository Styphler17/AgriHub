Fix CORS issues and modernize build process

- Whitelisted http://localhost:3000 for Dexie Cloud authentication.
- Replaced Tailwind CSS CDN with native Vite/NPM integration.
- Consolidated custom styles and fonts into index.css.
- Removed external CDN assets from sw.js to fix Service Worker fetch errors.
- Cleaned up index.html by removing import maps and CDN scripts.
