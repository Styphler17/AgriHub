In the MarketPrices component, when a price alert is triggered, show a more prominent notification (e.g., a toast message or banner) to the user, even if they are not actively viewing the MarketPrices component.
In the MarketPrices component, provide options to export the filtered and sorted market data in CSV format, in addition to the existing PDF export.

In the MarketPrices component, implement a feature to refresh market prices automatically every 5 minutes, but only if the user is online.

In the MarketPrices component, add unique icons for each commodity (e.g., a maize icon for Maize, a cocoa pod icon for Cocoa) next to the commodity name in the list and detail view.